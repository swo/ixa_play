# ixa play

Run the model and view the prevalence report with `make`. Use `make LOG=TRACE` to get the trace.

This is a sandbox, so I just change the code willy-nilly. Different states of the code are tracked in the PRs.
